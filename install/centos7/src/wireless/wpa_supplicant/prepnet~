#!/bin/sh

# /etc/init.d/prepnet
# ln -s /etc/init.d/prepnet /etc/rc.d/rc3.d/S09PREPNET
# ln -s /etc/init.d/prepnet /etc/rc.d/rc5.d/S09PREPNET
# chkconfig Networkmanager off
# chkconfig network on
# chkconfig messagebus on
# chkconfig wpa_supplicant on

killall dhclient >/dev/null 2>&1
dhclient ra0

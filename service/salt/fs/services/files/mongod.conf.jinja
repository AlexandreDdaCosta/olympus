{% set trim_blocks=True -%}
# mongod.conf

# For documentation of all options, see:
# http://docs.mongodb.org/manual/reference/configuration-options/

# where and how to store data
storage:
  dbPath: /var/lib/mongodb
  journal:
    enabled: true

# where to write logging data
systemLog:
  destination: file
  logAppend: true
  path: /var/log/mongodb/mongod.log
  component:
    accessControl:
      verbosity: 2
    index:
      verbosity: 1
    network:
      verbosity: 2
    write:
      verbosity: 1

# network interfaces
net:
  port: 27017
  tls:
   mode: preferTLS
   certificateKeyFile: /etc/ssl/localcerts/server-key-crt.pem
   CAFile: /etc/ssl/localcerts/ca-crt.pem

#processManagement

{%- if auth_enabled_count == 1 %}
security:
  authorization: enabled.
{% else %}
#security:
#  authorization: enabled.
{%- endif %}

#operationProfiling:

#replication:

#sharding:

## Enterprise-Only Options:

#auditLog:

#snmp:
